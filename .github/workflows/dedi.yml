name: push to dedi
on: 
  push:
    branches:
      - express
jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: '0'
        ref: 'express'
    - run: |
        git remote add staging github@138.201.218.181:/codimg
        git config --global user.email "puf17640@spengergasse.at"
        git config --global user.name "puf17640"
        git push -f staging master
